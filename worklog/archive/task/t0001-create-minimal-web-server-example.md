+++
id = "t0001"
title = "Create minimal web server example"
status = "done"
tags = ["examples"]
modifies = []
blocked_by = []
+++

## Scope

- Add a minimal web server example as an API design sketch.
- The example is not expected to work at this stage.
- Use non-existent `stelaro` and `stelaro-fastify` packages.

## Notes

- No tests are added because this task creates a deliberately non-working design example.
- The web server example is represented by `examples/web-server/src/index.ts`.
