+++
id = "t0002"
title = "Add web server counter example"
status = "done"
tags = ["examples"]
modifies = []
blocked_by = []
+++

## Scope

- Add a counter to the web server example.
- The counter is managed by a counter component.
- The count is stored in memory and is not persisted.
- Clicking a button increases the shared count by one.

## Notes

- No tests are added because the example is still a deliberately non-working API design sketch.
- The counter state is represented inside the sketched counter component API.
